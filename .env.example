# Copy to .env and edit before the first start.

# Required. Gates the whole dashboard UI and API.
# Generate a strong one:  openssl rand -base64 24
ADMIN_PASSWORD=change-me-before-first-start

# Optional
TZ=Pacific/Auckland
